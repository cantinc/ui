Use the `gap` prop to add spacing between child elements.
It accepts a `number` for uniform gaps or an array `[number, number]` for different gaps along the main and cross axes.
By default, it is 0 (no gap).
