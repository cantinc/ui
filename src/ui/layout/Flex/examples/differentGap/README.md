You can set different gaps for the main axis (horizontal by default) and cross axis (vertical by default) by passing an array `[mainGap, crossGap]` to the `gap` prop.
In this example, with wrapping enabled, `gap={[16, 32]}` applies **16px** spacing between items horizontally and **32px** vertically between wrapped lines.
