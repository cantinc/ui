The `flex` prop allows child elements to grow and fill available space.
It accepts a `number` (for proportional growth, where `flex={1}` means an equal share of remaining space) or a `boolean` (where `true` is equivalent to `flex={1}`).
